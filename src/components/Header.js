import { h } from 'preact';

const Header = () => (
  <header class="page-header">
    <a href="/">Typing Game</a>
    <nav>
      <a href="/">Home</a>
    </nav>
  </header>
);

export default Header;
